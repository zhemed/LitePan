# research.md — 存量批次字段清理：执行记录（结论：无需写入）

## 1. 入口条件（R1）

用户 2026-09-11 告知「已升级，并验证成功文件传输完毕」，据此启动（`task.py start`）并做只读确认：

| 检查 | 结果 |
|---|---|
| 生产机镜像 | `ghcr.io/zhemed/litepan:v0.0.37`，ImageID `sha256:36b11f22c230…`，容器启动 `2026-09-10T16:45:29Z`（= **2026-09-11 00:45:29 CST**） |
| 任务状态 | `success 819` / **`failed 0` / `paused 0` / `pending 0` / `running 0`** |
| 任务创建时间 | `2026-09-11 00:46:08 ~ 00:50:10`（升级后新运行创建） |
| 最近完成 | `2026-09-11 01:49:46`（`vzdump-qemu-101-…vma.zst`） |

## 2. 关键发现：**无需清理**（`auto-%` 行为 0）

| 查询 | 结果 |
|---|---|
| `select count(*) from upload_tasks where batch_id like 'auto-%'` | **0** |
| `batch_id` 非空行数 | **0** |
| `batch_name` 非空行数 | **0** |
| 任务总数 | 819 |

原因：用户在批次结束后已升级到 0.0.36/0.0.37 并触发**新的一次运行**——该版本按 0.0.36 决策**不再写入批次身份**，因此新任务的 `batch_id/batch_name` 天然为空；而 0.0.35 那批带批次字段的任务已不在库中（用户已清理/替换）。⇒ **任务面板当前即为逐文件平铺**，本次清理目标已由版本行为自然达成。

## 3. 执行动作（最小写入原则）

| 步骤 | 执行情况 |
|---|---|
| 备份 DB | **未执行**——因为没有发生任何写入（无目标行），备份无对象 |
| `UPDATE ... WHERE batch_id LIKE 'auto-%'` | **未执行**——前置 `SELECT` 计数为 0，按 PRD「最小写入」原则不做无对象写入 |
| 其它写操作 | 无（本轮仅 `docker inspect` + SQLite `mode=ro` 的 `SELECT`） |
| 容器/配置/队列 | 未触碰 |

## 4. 与预期风险的对照（PRD-R5）

原风险评估针对「清空 `batch_id` 后」的两处读取方（熔断分组键退化、批次根删除守卫退化）。由于**实际未发生写入**，两者均不受影响；生产库两列本就为空。

## 5. 结论

- 本次任务以「**目标已达成、无需写入**」闭环，无副作用。
- 用户验收事实：819 个文件**全部成功、零失败**；面板无批次折叠（无批次字段）。
- 后续：新一代版本（≥0.0.36）自动化运行不再产生批次字段，此问题不会复发；若将来又在面板看到折叠批次，那是浏览器批量上传场景（`client_task_id` 作为批次号），终态桶问题已由 0.0.37 修复。
