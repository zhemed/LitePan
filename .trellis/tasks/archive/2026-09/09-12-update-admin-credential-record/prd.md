# 更新 AGENTS.md 的管理员口令记录到本机实例（2026-09-12 改密）

## Goal

用户已于 2026-09-12 在本机新部署的 `:5211` 实例上完成改密。`AGENTS.md:60` 的口令记录**仍停留在上一台机器的状态**（旧盐哈希 `aa9764…`、指向一个本机不存在的备份路径）。本任务把该记录更新为**本机现状**，并保留口令变更史，供后续会话/维护者准确判断。

## Background（2026-09-12 实测）

| 核实项 | 结果 |
|---|---|
| 新口令 `admin / 123456` | **HTTP 200** 登录成功，`must_change_password:false`（强制改密已解除） |
| 旧口令 `admin / admin` | **HTTP 401**（已失效，改密确实生效） |
| 当前哈希 | `pbkdf2:sha256:600000$af0dac85f9dad16…`（与旧记录的 `aa9764…` 不同盐） |
| 库位置 | `/root/LitePan/data/litepan.db`（本机部署，2026-09-12） |
| 现有记录原文 | `AGENTS.md:60`：`当前线上管理员：admin / 123456（2026-09-09 经用户授权重置落库 pbkdf2:sha256:600000$aa9764...，重置前备份 /tmp/litepan-backup-20260909-201827.db；此前 08-30 记录的 admin/admin 已作废），后续改密必先建 Trellis 任务并 ask_user_question。` |

**旧记录的两处失准**（本任务要修的正是这两点）：
1. 哈希 `aa9764…` 属于**上一台机器**的库；本机新库是 `af0dac85…`（同口令不同盐）。
2. 记录的备份路径 `/tmp/litepan-backup-20260909-201827.db` **不在本机**（已于早前任务实测确认），照它找备份会扑空。

## Requirements

### D1 更新为"本机现状"为主记录

- 主记录明确：口令 `admin / 123456`、**本机** `:5211` 实例、`2026-09-12` 经界面改密落库、哈希前缀 `af0dac85f9dad16…`、库位置 `/root/LitePan/data/litepan.db`、`must_change_password` 已解除
- 保留既有流程约束原文：**后续改密必先建 Trellis 任务并 `ask_user_question`**

### D2 保留口令变更史（防止后续会话混淆）

按时间线保留，并标注哪些属于**别的机器**、哪些在本机不存在：

- `08-30`：`admin/admin`（已作废）
- `2026-09-09`：上一台机器重置为 `123456`，旧盐哈希 `pbkdf2:sha256:600000$aa9764…`，当时备份 `/tmp/litepan-backup-20260909-201827.db` —— **该备份不在本机**
- `2026-09-12`：本机新库改密为 `123456`，新盐哈希 `pbkdf2:sha256:600000$af0dac85f9dad16…`，库在 `/root/LitePan/data/litepan.db`

### D3 不改其它内容

- `AGENTS.md` 其余段落（强制序列、门禁命令、API 登录表单编码提示、版本基线、关联任务）**一律不动**

## Constraints

- **只改 `AGENTS.md` 的口令记录一处**（第 60 行为主，必要时拆成两行）；不改其它任何文件
- **不改实际口令**：用户已在界面完成，本任务纯记录同步，不触碰 `data/litepan.db`
- **不改 `AGENTS.md` 中 `<!-- TRELLIS:START -->…<!-- TRELLIS:END -->` 托管块内的内容**（该块会被 `trellis update` 覆盖）
- 记录须**可核对**：哈希前缀取自本机库实际值，不臆造
- 规则文件写操作，已按项目强制规则先建任务并完成到 `task.py start` 后再生效
- 不修改归档任务与历史 journal

## Acceptance Criteria

- [x] `AGENTS.md` 的口令记录含本机要素：`af0dac85f9dad16`、`2026-09-12`、`/root/LitePan/data/litepan.db` —— 实测三项逐一 grep 命中 ✅
- [x] `AGENTS.md` 不再把 `aa9764…` 呈现为**当前**哈希 —— 实测 `aa9764` 仅出现在新增的「口令变更史」行，且已标注为**上一台机器**的旧盐哈希
- [x] `AGENTS.md` 明确标注 `/tmp/litepan-backup-20260909-201827.db` **不在本机** —— 实测变更为"当时备份 …，**该备份不在本机**"
- [x] 流程约束原文仍在：`后续改密必先建 Trellis 任务并 ask_user_question` —— 实测命中
- [x] `git diff --stat AGENTS.md` 仅改动口令记录相关行 —— 实测 `1 file changed, 2 insertions(+), 1 deletion(-)`，即原 1 行替换为 2 行；其它段落零改动
- [x] `<!-- TRELLIS:START -->…<!-- TRELLIS:END -->` 托管块**零改动** —— 实测 diff 中该块标记出现次数 = **0**（不在托管块内编辑，符合 `trellis update` 约定）
- [x] 运行中实例未受影响：`/api/health` 仍 200、容器仍 `Up`；`data/litepan.db` 未被本任务触碰 —— 实测 `litepan | Up 15 minutes`、health 200；DB mtime 仍为基线值 `2026-09-12 12:22:03.940111862`（**与任务开始前完全一致**，证明未触碰）
- [x] `git status --short` 仅 `AGENTS.md` + 本任务目录 —— 实测无越界文件

## Notes

- Scope 标注 `lightweight`：单文件、单处记录同步，无技术方案分歧，故 PRD-only。
- **风险事实（已在提问中明示，用户知情后选择照旧记录）**：`zhemed/LitePan` 为 **public** 仓库，`AGENTS.md` 会随仓库公开，故此记录等同于公开 `admin/123456`；实例监听 `0.0.0.0:5211`。用户答复为"这个是给你维护用的，你记一下就行了"，本任务据此执行；该事实一并写入任务档案备查。
- 本任务**不**做：修改实际口令、轮换密钥、收紧网络暴露面。

## 检查记录（trellis-check，2026-09-12）

**Step 1 变更识别**：单文件 `AGENTS.md`，**2 insertions / 1 deletion**（原 1 行扩为 2 行）。零代码改动。

**Step 3 项目质量门**：代码质量门（lint/vet/test/type-check）**N/A** —— 纯规则文档同步，未触及工程代码。本任务的"质量门"是**记录准确性**：新哈希前缀取自本机库实际读取值（`af0dac85f9dad16…`），未臆造；口令可用性经实测（新口令 200、旧口令 401）。

**Step 4 清单核对**

- 测试覆盖：**N/A**（无可测行为）。规则文件无对应测试框架。
- Spec 同步：**无需** —— 本任务是同步已发生的事实到权威记录，未产生新约定。
- 范围纪律：仅口令记录一处；`AGENTS.md` 其它段落与 Trellis 托管块均零改动；未触碰 `data/litepan.db`（mtime 与基线逐位一致为证）。
- 跨层一致性：N/A。

**诚实记录**

1. **风险事实已明示、用户知情后选择照旧记录**：提问时我已指出 `zhemed/LitePan` 为 **public** 仓库（`gh api` 确认 `visibility: public`）、实例监听 `0.0.0.0:5211`、故 `AGENTS.md` 中的口令等同于公开凭据，并给出"不记凭据/只记哈希/先改强口令"三个替代选项。用户答复"这个是给你维护用的，你记一下就行了"，本任务据此执行。**该事实保留在任务档案中备查。**
2. **只记哈希也不构成保护**（已在提问中说明）：`123456` 的 pbkdf2 哈希可秒破，故若日后要收紧，有效手段只有"不记录 + 改强口令"，而非改记哈希。
3. **旧记录的两处失准已修正**：`aa9764…` 属上一台机器的库（同口令不同盐），且其备份路径 `/tmp/litepan-backup-20260909-201827.db` 本机不存在 —— 后者早在 `09-12-setup-dev-environment` 就实测确认，照旧记录找备份会扑空。变更史现已显式标注机器归属。
4. **未改动实际口令**：本任务纯记录同步；`admin / 123456` 由用户本人在界面完成设置。
