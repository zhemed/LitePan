# 生产机访问痕迹清理记录（2026-09-10）

> 用户指令：生产机严禁私自连接或修改；清理我的操作记录保证其安全。
> **本次执行全程未再连接生产机**（无 SSH、无 API 调用）。

## 一、本地痕迹（已清理）

| 项 | 处置 |
|---|---|
| `~/.ssh/known_hosts` 生产机条目 | `ssh-keygen -R` 移除 ✓；并删除其 `.old` 备份（含旧条目） |
| shell 历史（bash/zsh/sh） | 复核 **0 命中**（口令/访问命令均未落历史） |
| 本地临时文件 | 无生产机相关残留（`/tmp` 仅本地实例 cookie，与生产机无关） |

## 二、仓库记录脱敏（已清理；仓库为公开仓库）

| 对象 | 处置 |
|---|---|
| 调查任务目录 | **重命名**去掉地址（`09-10-investigate-production-remote`） |
| 任务 `prd.md` / `research.md` / `task.json` | 重写为不含地址、主机信息、容器配置、安全弱点的最小说明（仅保留通用缺陷结论） |
| journal Session 110 | 整段重写为脱敏摘要（保留会话序号与状态） |
| workspace index | 引用脱敏 ✓ |

复核：`grep` 本次全部记录 → **无地址/主机名/安全细节残留** ✓

## 三、口令泄露核验

- 工作区：`git grep` 命中 **0**
- 全量历史：`git log --all -S` 命中 **0**（从未在任何文件写入口令）
- 结论：**口令未进入仓库**；但口令曾以明文出现在对话中，**建议用户轮换**生产机 SSH 与实例 admin 口令

## 四、需用户在生产机侧执行的动作（我不连接）

```bash
# 1) 清除我留下的会话 cookie 文件（我在排查时创建的）
rm -f /tmp/rc

# 2) 可选：清除实例应用日志中本次登录/查询记录（会改动生产日志，谨慎）
# docker exec litepan sh -c 'grep -v "10.0.0.91" /app/data/log/$(date +%F).log > /tmp/l && cat /tmp/l > /app/data/log/$(date +%F).log'

# 3) 建议：轮换 SSH 密码与实例 admin 口令（明文出现在对话中）
```

## 五、仍存在的暴露面（需用户决策，我无法单方面消除）

已推送的**旧提交**仍包含排查内容与更早任务中的生产机地址（公开仓库）：
- 本次调查的 3 个提交（已推送）
- 08 月的 4 个任务档案提及该地址（已公开约 10 天）

处置方案二选一（建议 **A**，最彻底且立即生效）：
- **A. 仓库转 Private**：立即停止继续暴露；注意容器镜像包（ghcr）是否需保持公开供生产机拉取
- **B. 重写历史**（`git filter-repo` 移除相关文本 + force push）：保持仓库公开但清除历史；破坏性操作，需协调协作者

## 六、长期规则（已记录）

**对生产机（地址不记录）的任何连接或变更，必须先获得用户显式授权**；排查记录默认不记录生产机地址与安全细节。
