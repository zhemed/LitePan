# 115错码430004映射NotFound作用调查报告

调查时间：2026-09-05 · 只读 · 上游 `b2ecd58`（同提交另动 STRM 扫描器，本地 STRM 已删）

## 结论

**建议合：唯一行为级收益是"删除幂等"，风险≈0，搭下次顺风车（不单独发版）。** 所谓 nicety 其实只有一半 nicety——删除路径是真行为差异。

## 位置与惯例

- `mapResponseError` switch 首位（`406限流`、`isAuthCode` 之前），消息格式与相邻分支一致（`"115 API 错误(%d)：%s"`）。
- 首位只是防御性写法：430004 不以 401 开头、不等于 406，本来也只会进 default；放首位不影响任何其他分支。
- 所有 115 API 调用（删/列/传/建目录）经 `apiCall→decode→mapResponseError`（transport.go:135），故 430004 可在任一操作中出现，语义=**目标云端已消失**（具体官文定义未能独立验证：115 未公开完整错码表；由同提交 STRM 扫描器语境"扫到消失文件" + NotFound 映射推断）。

## 有/无行为差异表

| 触发点（115目标消失） | 无映射（现状） | 有映射 | 是否真差异 |
|---|---|---|---|
| 删除文件 | `DRIVER_ERROR`/502，UI"删除失败" | `NOT_FOUND`/404，`file/service.go:229` 日志"视为成功" | ✅ 是（幂等删除） |
| 面板列取 | 502 通用失败 | 404"文件不存在" | 仅消息+状态码 |
| 服务端上传建目录中 List（target_dir.go:97 直接 return err） | 任务失败 | 任务失败 | 仅消息 |
| 认证/重试/调度器 | 430004 不进 `isAuthCode`，不误判 | 同左 | 无 |

## 风险

- 收敛面=仅 `code==430004` 一个码；认证码（99/401/401xxx）与限流码（406）不受影响（前缀不重叠）。
- `apply --check` 已验证通过（上个任务）；无 115 账号，合入后依然无法真机验证该分支（与现状一致，不新增验证债）。
