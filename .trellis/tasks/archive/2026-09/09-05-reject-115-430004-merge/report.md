# 否决 115错码430004映射合入 · 决定归档

决定时间：2026-09-05 · 决定人：用户 · 状态：**否决，不合入**

## 决定

否决上游 `b2ecd58` 中 `drivers/115_Open/transport.go` 的 `430004→CodeNotFound` 2 行合入（`case code == 430004: return domain.Errorf(domain.CodeNotFound, ...)`）。

## 理由（用户拍板）

1. **STRM 已整体移除**：该映射是为 STRM 扫描"云端目录消失则跳过+防误删"（上游配套 130 行）而生的传感器；执行器不存在，最大价值无处落地。
2. **无 115 账号**：全库唯一账号为天翼云盘，115 驱动无触发方，合入后分支永久零覆盖。
3. **总体零收益**：本地仅剩删除幂等一路边角收益（删已消失文件少报一次失败）+ 提示语 502→404；列取/上传/认证/重试行为均不变。

## 调查链（备查，两次）

- `09-05-check-115-430004-mapping`：链路追踪（transport→file/service→target-dir），差异表。
- `09-05-recheck-115-430004-effect`：读透 STRM 配套逻辑得真实故事；4 路搜索无 115 官方错码表（官文定义未能独立验证）；合否理由各四。

## 重开条件（任一满足可重开执行任务）

- 绑定 115 账号且出现"已删又删报失败"等真实 complaint；
- 重引入任何消费 `CodeNotFound` 的 115 链路（扫描/对账/同步）。
- 重开成本：2 行，`apply --check` 已验证通过，可搭顺风车，无需单独发版。

## 变更记录

纯文档归档，业务代码零改动，无版本/tag/镜像变动。
