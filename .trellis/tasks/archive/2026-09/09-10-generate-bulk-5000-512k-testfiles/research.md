# 生成记录（2026-09-10 20:04）

| 项 | 结果 |
|---|---|
| 路径（宿主） | `/root/LitePan/mounts/LitePan-123/bulk-5000-512k/` |
| 路径（容器/面板填写） | `/app/mounts/LitePan-123/bulk-5000-512k` |
| 文件 | `bulk5k_0000.bin` ~ `bulk5k_4999.bin`，**5000 个** |
| 字节 | 单文件 524288 B；**总计 2,621,440,000 B（2.44GiB）**，Python 精确复核一致 |
| 唯一性 | 全量 MD5：**5000 行 / 5000 唯一**（无秒传风险），耗时 3.2s |
| 生成方式 | 单次 `head -c 2560M /dev/urandom \| split -b 512K -d -a 4`（5.2s），随后裁掉多余的 120 个（5120 → 5000） |
| 容器可见性 | 容器内 `ls` 实测 5000 项、单文件 524288 ✓ |
| 磁盘 | 生成后 225G 可用（占用 2.5G） |

## 复测参考（0.0.29，并发=1 + 500ms 账号间隔门）

- 512KiB 文件：init/commit 各过一次门（≈1s），数据面 PUT 不过门（不到 1s），目录解析命中缓存 → 预估 **~1.2-1.8s/文件**
- 全批 5000 个：**约 100-150 分钟**（对照此前 1MiB×2000 的 50 分钟 ≈ 40-60/分钟；512KiB 更小，速率应更快）
- 关注点：批次树渲染（窗口化后默认只带最近 500 已完成）、暂停/继续批量操作是否流畅（0.0.25/0.0.29 已修）、徽标计数是否准确（0.0.29 分桶）
