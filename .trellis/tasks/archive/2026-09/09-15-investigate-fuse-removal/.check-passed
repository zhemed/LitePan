checked_at=2026-09-15 12:42:42
note=只读调查完成，产物 research.md（10 节，含可复跑命令）。结论：FUSE 可彻底移除。决定性证据：① 前端 web/src/api/fuse.ts 11 个导出里只有 fetchFuseMounts 被调用（1 处，仪表盘只读卡片），其余 10 个零调用 → 功能只有管理侧、无消费侧，且连创建入口都没有；② 实例运行期实测：fuse_mounts 0 行、status enabled=false、mounts=[]、读缓存 block_count=0/used_bytes=0、宿主与 /proc/mounts 无 FUSE 挂载、容器内 /app/mounts 空、全量日志零 FUSE 活动、通知表 0 行、设置页实测不渲染 FUSE 读缓存项（category=performance 不在渲染分组）；③ FUSE 是唯一需要 privileged + pid:host + /dev/fuse 的功能（全仓 /proc 读取仅 fusemount/mountpoint_linux.go 一处，fusermount3 仅它调用）→ 删除后容器可退回非特权。盘清：32 个受跟踪文件、后端 3634 行（含 234 行测试）+ 前端 85 行，另需修改 20 处（含必须改的 internal/store/backup.go BackupCounts 的 tables 列表，漏了会让备份报错）；构建标签矩阵实测 -tags fuse 只是关实现而非移除（无标签时 fuse 三包仍在依赖图中、go-fuse 子包 9→0）。二级连锁：playback.OpenRemoteReader 唯一调用者是 share/fuse/nodes.go:393 → RemoteReader/remoteWindowReader 圈将成为死代码（建议另开任务处置）。反例清单已列（历史迁移 0008 保留、playback HTTP 链路与 LocalUpload 的 TempRegistry 不删、LocalFs 驱动无关）。零写操作：git status 受跟踪改动 0，容器/镜像/DB 未动（migration 24、fuse_mounts 0 行不变）；spec 15 处待同步已登记未修改（按 PRD 约束只登记）。两工具盲区复核：deadcode 7 行中 FUSE 相关 0、unused 0 issues —— 印证『工具不报 ≠ 还在用』。
