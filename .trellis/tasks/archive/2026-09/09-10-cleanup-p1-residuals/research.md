# P1 残留清理执行记录（2026-09-10 19:28-19:32）

## 逐步结果

| 步 | 操作 | 前 | 后 | 核验 |
|---|---|---|---|---|
| A | 删云端 `bulk-test-2000` 树（外层 723041273726025674，内层 190 文件） | 目录+190 文件 | 已删除（进回收站） | root List 复查：**无 bulk-test-2000** ✓ |
| B | 备份 DB + 删 1810 个暂停任务记录（分 5 块批量 API） | 7814 行（paused 1810 + success 6004） | **6004 行** | 失败 0；备份 `data/backups/manual-pre-p1-20260910-192849.db`（4.2M） |
| C | 删本地测试目录（2000 文件） | 2.0G | 不存在 | 磁盘 11G → **8.8G**（释放 2.0G+） |
| D | 删残留容器 `litepan-auto` + 镜像 `litepan-own:0.0.8` | Exited 2 周 | 均删除 | 容器数 2 → 1（仅运行中 litepan） |
| E | 镜像 tag 治理：清 75 个历史 tag（保留 latest/0.0.27/0.0.26/litepan-go:dev） | 36 镜像 / 1.059GB | **3 镜像 / 169.7MB** | 保留清单在位；回收 ≈890MB |
| F | 收尾核验 | — | — | 服务三连 ✓；容器 StartedAt `11:08:20Z` **未变**；汇总 API `total:6004` ✓ |

## 安全核验

- 运行容器、其镜像（`litepan:latest`）、卷：**全程未触碰**
- 删除的均为测试残留（云端测试树、本地测试文件、测试任务记录、历史实验镜像）
- DB 清理前已备份；云端删除走官方 API

## 遗留说明

1. **云端 190 个测试文件现位于 189 回收站**（账号 `delete_mode=trash`，App 无回收站清空入口）——会在 189 侧按策略自动过期；如需立即释放配额，需在 189 客户端清空回收站，或临时把账号删除模式切 `delete` 后再触发（需另行确认）。
2. Build Cache 仍有 1.01GB（0.0.27 构建残留，RECLAIMABLE 918MB）——如需可再跑一次 `docker builder prune -f`。
3. P2/P3 未做：旧 DB 备份（`litepan.db.bak.1788077861` 229K）、spec 契约缺口（0.0.27 窗口化 API）、/tmp 杂物、批次树记忆化、工作集保留策略。
