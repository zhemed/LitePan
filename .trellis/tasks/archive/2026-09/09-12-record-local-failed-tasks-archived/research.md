# research.md — 本地 3 条失败任务归档确认（只读复核）

## 1. 用户确认

> 「本地那 3 条失败任务确认归档」——用户（2026-09-12）。

## 2. 只读复核结果（本地实例，`/root/LitePan/data/litepan.db`，`mode=ro`）

| 检查项 | 结果 |
|---|---|
| 容器/镜像 | `ghcr.io/zhemed/litepan:latest` = ImageID `sha256:36b11f22c230…`（对应 **v0.0.37**），`Restarts=0` |
| 任务总数 | **13** |
| 状态分布 | **`success 13`**（`failed 0 / paused 0 / pending 0 / running 0`） |
| 任务创建时间 | `2026-09-12 00:01:15`（同一秒创建，属一次小批次） |

### 与 2026-09-11 失败清单逐一对照

| 文件 | 2026-09-11 状态 | 2026-09-12 复核 | 说明 |
|---|---|---|---|
| `bulk5k_1609.bin` | failed（`HTTP 511 S3ClientException`） | **success** | 重传成功 ✅ |
| `bulk5k_1540.bin` | failed（`HTTP 511 S3ClientException`） | **success** | 重传成功 ✅ |
| `bulk5k_1267.bin` | failed（`HTTP 513 NoSuchUpload`） | **无记录** | 任务记录已随列表清理归档 |

⇒ 当前 `failed` 计数为 **0**，用户的「归档」确认与库内状态一致。

## 3. 观察（如实标注，非我方改动）

- 本地任务记录规模由 **15,098 → 13**（15,098 为本项目 08-30 起累积的测试/复测任务记录，含 bulk 测试文件）。
- 该变化**不是我方操作**：本会话对本地实例的写操作只有「容器重建」（镜像升级），未删除任何任务记录；记录清理发生在用户侧。
- **不影响云端数据**：任务记录删除 ≠ 删除网盘文件（云端文件保持在 天翼云盘）；`mounts/LitePan-123/bulk-*` 等本地测试文件亦未受影响。

## 4. 结论与后续

- 3 条失败任务：**2 条已重传成功、1 条记录已清理**，用户确认归档 ⇒ 本事项闭环。
- 189 侧 `HTTP 511/513`（S3 分片）属上游瞬时异常，已由重传解决；若后续频繁出现，另开任务做专项（含 0.0.20/0.0.21 既有的传输重试策略复查）。
